<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Onboarding Journey</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; }
        .custom-scrollbar::-webkit-scrollbar { width: 6px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: #f1f5f9; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 9999px; }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
        @keyframes slideInRight {
            from { opacity: 0; transform: translateX(50px); }
            to { opacity: 1; transform: translateX(0); }
        }
        .animate-slide-in {
            animation: slideInRight 0.3s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }
    </style>
</head>
<body class="bg-slate-50 text-slate-900 min-h-screen flex flex-col antialiased">

    <!-- FLOATING TOAST NOTIFICATION CONTAINER (TOP-RIGHT) -->
    <div id="toast-container" class="fixed top-20 right-4 sm:right-6 z-50 flex flex-col gap-3 max-w-sm w-full pointer-events-none px-4 sm:px-0"></div>

    <!-- ===================================================== -->
    <!-- 1. CREATE / SWITCH PROFILE MODAL                       -->
    <!-- ===================================================== -->
    <div id="login-overlay" class="hidden fixed inset-0 bg-slate-900/75 backdrop-blur-md z-50 flex items-center justify-center p-4">
        <div class="bg-white p-8 md:p-10 rounded-3xl shadow-2xl max-w-md w-full border border-slate-100 text-center relative overflow-hidden">
            <div class="absolute top-0 left-0 right-0 h-2.5 bg-gradient-to-r from-blue-600 via-indigo-600 to-emerald-500"></div>

            <div class="w-16 h-16 bg-blue-50 text-blue-600 rounded-2xl flex items-center justify-center mx-auto mb-5 shadow-sm text-2xl font-bold">
                🛡️
            </div>

            <h2 class="font-extrabold text-2xl text-slate-900 mb-1 tracking-tight">Your Onboarding Profile</h2>
            <p class="text-slate-500 text-sm mb-6">Enter your name and work email to personalize your journey and create your profile.</p>

            <form onsubmit="event.preventDefault(); handleProfileSubmit();" class="space-y-4">
                <div class="text-left">
                    <label class="block text-xs font-bold uppercase tracking-wider text-slate-500 mb-1.5 ml-1">Full Name</label>
                    <input type="text" id="employee-name-input" value="Shraddha Warade" 
                           class="w-full px-4 py-3.5 border border-slate-200 rounded-xl text-slate-800 placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition bg-slate-50 focus:bg-white text-sm" 
                           placeholder="e.g. Shraddha Warade" required>
                </div>

                <div class="text-left">
                    <label class="block text-xs font-bold uppercase tracking-wider text-slate-500 mb-1.5 ml-1">Email Address</label>
                    <input type="email" id="employee-email-input" value="shraddhawarade85@gmail.com" 
                           class="w-full px-4 py-3.5 border border-slate-200 rounded-xl text-slate-800 placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition bg-slate-50 focus:bg-white text-sm" 
                           placeholder="e.g. shraddhawarade85@gmail.com" required>
                </div>

                <button type="submit" id="login-btn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3.5 rounded-xl shadow-lg shadow-blue-500/25 transition duration-200 flex items-center justify-center gap-2">
                    <span>Save & Launch My Workflow</span>
                    <span class="text-lg">→</span>
                </button>
            </form>

            <p id="login-status" class="text-xs mt-4 font-semibold text-slate-500 min-h-[20px]"></p>

            <div class="mt-6 pt-5 border-t border-slate-100 text-xs text-slate-400 flex items-center justify-center gap-2">
                <button onclick="document.getElementById('login-overlay').classList.add('hidden')" class="text-blue-600 font-semibold hover:underline">Close</button>
                <span>•</span>
                <span>Enterprise Onboarding Experience</span>
            </div>
        </div>
    </div>

    <!-- ===================================================== -->
    <!-- 2. TOP NAVIGATION BAR                                 -->
    <!-- ===================================================== -->
    <header class="bg-white border-b border-slate-200 sticky top-0 z-30 shadow-xs">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between">
            <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-gradient-to-tr from-blue-600 to-indigo-600 text-white rounded-xl flex items-center justify-center font-bold text-lg shadow-sm">
                    🚀
                </div>
                <div>
                    <span class="font-extrabold text-lg text-slate-900 tracking-tight">OnboardHub</span>
                    <span class="hidden sm:inline-block ml-2 text-xs font-semibold px-2.5 py-0.5 bg-blue-50 text-blue-700 rounded-md">Enterprise Portal</span>
                </div>
            </div>

            <div class="flex items-center gap-4">
                <button onclick="fetchLiveEmployeeData(true)" title="Sync live progress" class="flex items-center gap-1.5 text-xs font-semibold text-slate-600 hover:text-blue-600 bg-slate-100 hover:bg-blue-50 px-3.5 py-1.5 rounded-lg transition border border-slate-200">
                    <span class="text-sm">🔄</span>
                    <span class="hidden sm:inline">Sync Status</span>
                </button>

                <!-- Dedicated Notification Bell -->
                <div class="relative">
                    <button onclick="toggleNotificationDropdown()" id="notif-bell-btn" title="View System Notifications" class="relative flex items-center justify-center w-9 h-9 rounded-lg bg-slate-100 hover:bg-blue-50 text-slate-600 hover:text-blue-600 border border-slate-200 transition">
                        <span class="text-base">🔔</span>
                        <span id="notif-badge" class="absolute -top-1 -right-1 min-w-[18px] h-[18px] px-1 bg-rose-500 text-white text-[10px] font-extrabold rounded-full flex items-center justify-center ring-2 ring-white">
                            1
                        </span>
                    </button>

                    <!-- Notification Dropdown Menu -->
                    <div id="notif-dropdown" class="hidden absolute right-0 mt-2 w-80 sm:w-96 bg-white rounded-2xl shadow-2xl border border-slate-200 z-50 overflow-hidden">
                        <div class="p-3.5 bg-slate-50 border-b border-slate-100 flex items-center justify-between">
                            <div class="flex items-center gap-2">
                                <span class="font-bold text-xs text-slate-800 uppercase tracking-wider">System Notifications</span>
                                <span id="notif-count-label" class="text-[10px] font-bold px-2 py-0.5 bg-blue-100 text-blue-700 rounded-full">1</span>
                            </div>
                            <button onclick="clearNotifications()" class="text-[11px] text-slate-500 hover:text-rose-600 font-semibold transition">Clear all</button>
                        </div>
                        <div id="notif-dropdown-list" class="max-h-72 overflow-y-auto divide-y divide-slate-100 custom-scrollbar">
                            <div class="p-4 text-center text-xs text-slate-400">Loading notifications...</div>
                        </div>
                    </div>
                </div>

                <div class="flex items-center gap-3 pl-3 border-l border-slate-200">
                    <div id="user-avatar" class="w-9 h-9 rounded-full bg-indigo-600 text-white font-bold flex items-center justify-center text-xs shadow-xs">
                        SW
                    </div>
                    <div class="hidden md:block text-left">
                        <div id="nav-user-name" class="text-xs font-bold text-slate-800 leading-tight">Shraddha Warade</div>
                        <div id="nav-user-email" class="text-[11px] text-slate-500">shraddhawarade85@gmail.com</div>
                    </div>
                    <button onclick="openProfileModal()" title="Edit or create profile" class="text-xs font-semibold text-blue-600 hover:text-blue-700 bg-blue-50 px-2.5 py-1 rounded-lg border border-blue-200 ml-1">
                        Edit Profile
                    </button>
                </div>
            </div>
        </div>
    </header>

    <!-- ===================================================== -->
    <!-- 3. MAIN DASHBOARD CONTENT (SPLIT WORKSPACE)           -->
    <!-- ===================================================== -->
    <main class="flex-1 max-w-7xl w-full mx-auto p-4 sm:p-6 lg:p-8 flex flex-col lg:flex-row gap-6">

        <!-- ================================================= -->
        <!-- LEFT COLUMN: WORKFLOW PIPELINE & DASHBOARD        -->
        <!-- ================================================= -->
        <section class="flex-1 flex flex-col gap-6">

            <!-- DEDICATED HIGH-VISIBILITY NOTIFICATION BANNER (NOT IN CHAT) -->
            <div id="notification-banner" class="bg-gradient-to-r from-blue-50 via-indigo-50/70 to-emerald-50/70 border border-blue-200/90 p-4 sm:p-5 rounded-2xl shadow-xs flex items-start justify-between gap-4 transition-all duration-300">
                <div class="flex items-start gap-3.5">
                    <div class="w-10 h-10 rounded-xl bg-blue-600 text-white flex items-center justify-center font-bold text-lg shadow-sm shrink-0">
                        ⚡
                    </div>
                    <div>
                        <div class="flex items-center gap-2">
                            <span class="text-[11px] font-extrabold uppercase tracking-wider text-blue-700 bg-blue-100/80 px-2 py-0.5 rounded-md">Automated Action</span>
                            <span id="notif-banner-time" class="text-[11px] text-slate-400 font-medium">Just now</span>
                            <span class="text-[10px] font-bold px-2 py-0.5 bg-emerald-100 text-emerald-700 rounded-full">Recorded in Database</span>
                        </div>
                        <p id="notif-banner-text" class="text-sm font-semibold text-slate-800 mt-1 leading-snug">
                            Welcome email sent and profile recorded in database!
                        </p>
                    </div>
                </div>
                <button onclick="dismissNotificationBanner()" class="text-slate-400 hover:text-slate-700 p-1.5 rounded-lg hover:bg-slate-200/50 transition text-sm font-bold shrink-0" title="Dismiss notification">
                    ✕
                </button>
            </div>

            <!-- A. WELCOME & OVERALL PROGRESS CARD -->
            <div class="bg-gradient-to-br from-slate-900 via-slate-800 to-indigo-950 text-white p-6 sm:p-8 rounded-3xl shadow-xl relative overflow-hidden">
                <div class="absolute -right-16 -top-16 w-64 h-64 bg-blue-500/20 rounded-full blur-3xl pointer-events-none"></div>
                <div class="absolute -left-16 -bottom-16 w-64 h-64 bg-emerald-500/20 rounded-full blur-3xl pointer-events-none"></div>

                <div class="relative z-10 flex flex-col md:flex-row md:items-center md:justify-between gap-6">
                    <div>
                        <div class="inline-flex items-center gap-2 px-3 py-1 bg-white/10 backdrop-blur-md rounded-full text-xs font-semibold text-blue-200 mb-3 border border-white/10">
                            <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
                            <span>Onboarding Workflow Active</span>
                        </div>
                        <h1 class="text-2xl sm:text-3xl font-extrabold tracking-tight">
                            Welcome, <span id="hero-name" class="text-transparent bg-clip-text bg-gradient-to-r from-blue-200 to-emerald-200">Shraddha</span>! 👋
                        </h1>
                        <p class="text-slate-300 text-sm mt-1.5 max-w-xl">
                            Follow your connected workflow below. As you complete each milestone, the circle turns green and the pathway unlocks to the next stage.
                        </p>
                    </div>

                    <!-- Progress Gauge Badge -->
                    <div class="bg-white/10 backdrop-blur-md p-4 rounded-2xl border border-white/10 flex items-center gap-4 min-w-[210px]">
                        <div class="w-14 h-14 rounded-full border-4 border-emerald-400/30 border-t-emerald-400 flex items-center justify-center font-extrabold text-lg text-emerald-300" id="progress-percent">
                            40%
                        </div>
                        <div>
                            <div class="text-xs text-slate-300 uppercase font-semibold tracking-wider">Milestones</div>
                            <div class="text-base font-bold text-white" id="milestones-count">2 of 5 Completed</div>
                        </div>
                    </div>
                </div>

                <!-- Progress Bar -->
                <div class="mt-6 pt-5 border-t border-white/10">
                    <div class="w-full bg-white/10 rounded-full h-2.5 overflow-hidden">
                        <div id="main-progress-bar" class="bg-gradient-to-r from-blue-500 via-indigo-400 to-emerald-400 h-2.5 rounded-full transition-all duration-700 ease-out" style="width: 40%"></div>
                    </div>
                </div>
            </div>

            <!-- B. CONNECTED WORKFLOW PIPELINE (CIRCLES CONNECTED WITH MOVING GREEN LINE) -->
            <div class="bg-white p-6 sm:p-8 rounded-3xl border border-slate-200 shadow-xs">
                <div class="flex items-center justify-between mb-8">
                    <div>
                        <h2 class="font-extrabold text-lg text-slate-900 flex items-center gap-2">
                            <span>Onboarding Completion Workflow</span>
                            <span class="inline-flex items-center gap-1 text-[11px] font-bold px-2.5 py-0.5 rounded-full bg-emerald-50 text-emerald-700 border border-emerald-200">
                                <span class="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-ping"></span>
                                Live Workflow
                            </span>
                        </h2>
                        <p class="text-xs text-slate-500 mt-0.5">Circles turn green upon completion, advancing the green progress line to the next checkpoint</p>
                    </div>
                    <span class="text-xs font-semibold px-2.5 py-1 bg-slate-100 text-slate-600 rounded-full">Phase 1: Pre-boarding</span>
                </div>

                <!-- Horizontal Workflow Pipeline (Desktop) -->
                <div class="hidden md:flex items-start justify-between w-full relative px-2">

                    <!-- STEP 1: Profile Creation -->
                    <div class="flex flex-col items-center text-center w-28 shrink-0 cursor-pointer group" onclick="sendQuickPrompt('Tell me about my profile creation status')">
                        <div id="step-ball-1" class="w-14 h-14 rounded-full bg-emerald-500 text-white font-bold flex items-center justify-center shadow-lg shadow-emerald-500/25 ring-4 ring-emerald-100 text-xl transition-all duration-500 group-hover:scale-110">
                            ✓
                        </div>
                        <h3 class="text-xs font-bold text-slate-800 mt-3">1. Profile Setup</h3>
                        <span id="step-badge-1" class="mt-1 inline-block text-[10px] font-extrabold px-2 py-0.5 rounded-full bg-emerald-50 text-emerald-700 border border-emerald-200">
                            Completed
                        </span>
                        <p class="text-[11px] text-slate-400 mt-0.5">HR Profile Created</p>
                    </div>

                    <!-- CONNECTOR LINE 1 -> 2 -->
                    <div class="flex-1 mt-7 mx-2 relative">
                        <div class="h-2 bg-slate-200 rounded-full overflow-hidden">
                            <div id="flow-line-1" class="h-full bg-emerald-500 rounded-full transition-all duration-1000 ease-out" style="width: 100%;"></div>
                        </div>
                        <div id="flow-arrow-1" class="absolute -top-1.5 right-0 text-[10px] text-emerald-600 font-bold transition-colors duration-500">▶</div>
                    </div>

                    <!-- STEP 2: Background Verification (BGC) -->
                    <div class="flex flex-col items-center text-center w-28 shrink-0 cursor-pointer group" onclick="sendQuickPrompt('How do I complete my background verification (BGC)?')">
                        <div id="step-ball-2" class="w-14 h-14 rounded-full bg-amber-500 text-white font-bold flex items-center justify-center shadow-lg shadow-amber-500/25 ring-4 ring-amber-100 text-xl transition-all duration-500 group-hover:scale-110">
                            ⏳
                        </div>
                        <h3 class="text-xs font-bold text-slate-800 mt-3">2. Background Check</h3>
                        <span id="step-badge-2" class="mt-1 inline-block text-[10px] font-extrabold px-2 py-0.5 rounded-full bg-amber-50 text-amber-700 border border-amber-200">
                            Pending Review
                        </span>
                        <p class="text-[11px] text-slate-400 mt-0.5">Docs Verification</p>
                    </div>

                    <!-- CONNECTOR LINE 2 -> 3 -->
                    <div class="flex-1 mt-7 mx-2 relative">
                        <div class="h-2 bg-slate-200 rounded-full overflow-hidden">
                            <div id="flow-line-2" class="h-full bg-emerald-500 rounded-full transition-all duration-1000 ease-out" style="width: 0%;"></div>
                        </div>
                        <div id="flow-arrow-2" class="absolute -top-1.5 right-0 text-[10px] text-slate-300 font-bold transition-colors duration-500">▶</div>
                    </div>

                    <!-- STEP 3: Compliance Trainings -->
                    <div class="flex flex-col items-center text-center w-28 shrink-0 cursor-pointer group" onclick="sendQuickPrompt('What are my remaining pending trainings?')">
                        <div id="step-ball-3" class="w-14 h-14 rounded-full bg-blue-600 text-white font-bold flex items-center justify-center shadow-lg shadow-blue-600/25 ring-4 ring-blue-100 text-xl transition-all duration-500 group-hover:scale-110">
                            📚
                        </div>
                        <h3 class="text-xs font-bold text-slate-800 mt-3">3. Trainings</h3>
                        <span id="step-badge-3" class="mt-1 inline-block text-[10px] font-extrabold px-2 py-0.5 rounded-full bg-blue-50 text-blue-700 border border-blue-200">
                            1 of 4 Done
                        </span>
                        <p class="text-[11px] text-slate-400 mt-0.5">Mandatory Modules</p>
                    </div>

                    <!-- CONNECTOR LINE 3 -> 4 -->
                    <div class="flex-1 mt-7 mx-2 relative">
                        <div class="h-2 bg-slate-200 rounded-full overflow-hidden">
                            <div id="flow-line-3" class="h-full bg-emerald-500 rounded-full transition-all duration-1000 ease-out" style="width: 0%;"></div>
                        </div>
                        <div id="flow-arrow-3" class="absolute -top-1.5 right-0 text-[10px] text-slate-300 font-bold transition-colors duration-500">▶</div>
                    </div>

                    <!-- STEP 4: IT Credentials & Equipment -->
                    <div class="flex flex-col items-center text-center w-28 shrink-0 cursor-pointer group" onclick="sendQuickPrompt('When will I get my IT credentials and laptop?')">
                        <div id="step-ball-4" class="w-14 h-14 rounded-full bg-slate-100 border-2 border-slate-200 text-slate-400 font-bold flex items-center justify-center text-xl transition-all duration-500 group-hover:scale-110">
                            🔒
                        </div>
                        <h3 class="text-xs font-bold text-slate-800 mt-3">4. Work Credentials</h3>
                        <span id="step-badge-4" class="mt-1 inline-block text-[10px] font-extrabold px-2 py-0.5 rounded-full bg-slate-100 text-slate-500 border border-slate-200">
                            Locked
                        </span>
                        <p class="text-[11px] text-slate-400 mt-0.5">System Access</p>
                    </div>

                    <!-- CONNECTOR LINE 4 -> 5 -->
                    <div class="flex-1 mt-7 mx-2 relative">
                        <div class="h-2 bg-slate-200 rounded-full overflow-hidden">
                            <div id="flow-line-4" class="h-full bg-emerald-500 rounded-full transition-all duration-1000 ease-out" style="width: 0%;"></div>
                        </div>
                        <div id="flow-arrow-4" class="absolute -top-1.5 right-0 text-[10px] text-slate-300 font-bold transition-colors duration-500">▶</div>
                    </div>

                    <!-- STEP 5: Day 1 Ready -->
                    <div class="flex flex-col items-center text-center w-28 shrink-0 cursor-pointer group" onclick="sendQuickPrompt('What happens on Day 1?')">
                        <div id="step-ball-5" class="w-14 h-14 rounded-full bg-slate-100 border-2 border-slate-200 text-slate-400 font-bold flex items-center justify-center text-xl transition-all duration-500 group-hover:scale-110">
                            🚀
                        </div>
                        <h3 class="text-xs font-bold text-slate-800 mt-3">5. Day 1 Ready</h3>
                        <span id="step-badge-5" class="mt-1 inline-block text-[10px] font-extrabold px-2 py-0.5 rounded-full bg-slate-100 text-slate-500 border border-slate-200">
                            Upcoming
                        </span>
                        <p class="text-[11px] text-slate-400 mt-0.5">Team Launch</p>
                    </div>

                </div>

                <!-- Vertical Stepper on Mobile -->
                <div class="md:hidden flex flex-col gap-4">
                    <div class="flex items-center gap-4">
                        <div id="mob-ball-1" class="w-10 h-10 rounded-full bg-emerald-500 text-white font-bold flex items-center justify-center shrink-0">✓</div>
                        <div>
                            <div class="text-xs font-bold text-slate-800">1. Profile Setup</div>
                            <div class="text-[11px] text-emerald-600 font-semibold">Completed</div>
                        </div>
                    </div>
                    <div class="w-0.5 h-6 bg-emerald-500 ml-5 -my-2"></div>

                    <div class="flex items-center gap-4">
                        <div id="mob-ball-2" class="w-10 h-10 rounded-full bg-amber-500 text-white font-bold flex items-center justify-center shrink-0">⏳</div>
                        <div>
                            <div class="text-xs font-bold text-slate-800">2. Background Check (BGC)</div>
                            <div class="text-[11px] text-amber-600 font-semibold">Pending Review</div>
                        </div>
                    </div>
                    <div id="mob-line-2" class="w-0.5 h-6 bg-slate-200 ml-5 -my-2"></div>

                    <div class="flex items-center gap-4">
                        <div id="mob-ball-3" class="w-10 h-10 rounded-full bg-blue-600 text-white font-bold flex items-center justify-center shrink-0">📚</div>
                        <div>
                            <div class="text-xs font-bold text-slate-800">3. Compliance Trainings</div>
                            <div class="text-[11px] text-blue-600 font-semibold">1 of 4 Completed</div>
                        </div>
                    </div>
                    <div id="mob-line-3" class="w-0.5 h-6 bg-slate-200 ml-5 -my-2"></div>

                    <div class="flex items-center gap-4">
                        <div id="mob-ball-4" class="w-10 h-10 rounded-full bg-slate-200 text-slate-400 font-bold flex items-center justify-center shrink-0">🔒</div>
                        <div>
                            <div class="text-xs font-bold text-slate-800">4. Work Credentials</div>
                            <div class="text-[11px] text-slate-400 font-semibold">Locked</div>
                        </div>
                    </div>
                    <div id="mob-line-4" class="w-0.5 h-6 bg-slate-200 ml-5 -my-2"></div>

                    <div class="flex items-center gap-4">
                        <div id="mob-ball-5" class="w-10 h-10 rounded-full bg-slate-200 text-slate-400 font-bold flex items-center justify-center shrink-0">🚀</div>
                        <div>
                            <div class="text-xs font-bold text-slate-800">5. Day 1 Ready</div>
                            <div class="text-[11px] text-slate-400 font-semibold">Upcoming</div>
                        </div>
                    </div>
                </div>

            </div>

            <!-- C. MANDATORY TRAININGS CHECKLIST -->
            <div class="bg-white p-6 sm:p-8 rounded-3xl border border-slate-200 shadow-xs">
                <div class="flex flex-wrap items-center justify-between gap-2 mb-6">
                    <div>
                        <h2 class="font-extrabold text-lg text-slate-900 flex items-center gap-2">
                            <span>Mandatory Compliance Trainings</span>
                            <span id="trainings-badge" class="text-xs px-2.5 py-0.5 rounded-full font-bold bg-blue-50 text-blue-700">1 of 4 Completed</span>
                        </h2>
                        <p class="text-xs text-slate-500">All modules must be completed before credentials can be released</p>
                    </div>

                    <button onclick="askBuddyAboutTrainings()" class="text-xs font-semibold text-blue-600 hover:text-blue-700 bg-blue-50 hover:bg-blue-100 px-3 py-1.5 rounded-lg transition">
                        Ask Buddy for help →
                    </button>
                </div>

                <div id="trainings-grid" class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <!-- Training Cards will be populated dynamically -->
                </div>
            </div>

            <!-- D. KEY CONTACTS & SUPPORT DESK -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <!-- Manager Card -->
                <div class="bg-white p-5 rounded-2xl border border-slate-200 shadow-xs flex items-start gap-3.5">
                    <div class="w-10 h-10 rounded-xl bg-purple-50 text-purple-600 flex items-center justify-center font-bold text-lg shrink-0">
                        👤
                    </div>
                    <div>
                        <div class="text-[11px] uppercase font-bold text-slate-400 tracking-wider">Reporting Manager</div>
                        <div class="text-sm font-bold text-slate-800 mt-0.5">Sarah Jenkins</div>
                        <div class="text-xs text-slate-500">Engineering & Onboarding Lead</div>
                        <a href="mailto:sjenkins@company.com" class="text-xs text-blue-600 hover:underline mt-1 inline-block font-medium">sjenkins@company.com</a>
                    </div>
                </div>

                <!-- IT Helpdesk -->
                <div class="bg-white p-5 rounded-2xl border border-slate-200 shadow-xs flex items-start gap-3.5">
                    <div class="w-10 h-10 rounded-xl bg-blue-50 text-blue-600 flex items-center justify-center font-bold text-lg shrink-0">
                        💻
                    </div>
                    <div>
                        <div class="text-[11px] uppercase font-bold text-slate-400 tracking-wider">IT Provisioning</div>
                        <div class="text-sm font-bold text-slate-800 mt-0.5">IT Helpdesk</div>
                        <div class="text-xs text-slate-500">Hardware & System Credentials</div>
                        <a href="mailto:itsupport@company.com" class="text-xs text-blue-600 hover:underline mt-1 inline-block font-medium">itsupport@company.com</a>
                    </div>
                </div>

                <!-- HR Helpdesk -->
                <div class="bg-white p-5 rounded-2xl border border-slate-200 shadow-xs flex items-start gap-3.5">
                    <div class="w-10 h-10 rounded-xl bg-emerald-50 text-emerald-600 flex items-center justify-center font-bold text-lg shrink-0">
                        📋
                    </div>
                    <div>
                        <div class="text-[11px] uppercase font-bold text-slate-400 tracking-wider">BGC & Compliance</div>
                        <div class="text-sm font-bold text-slate-800 mt-0.5">People Operations</div>
                        <div class="text-xs text-slate-500">Document Verification Support</div>
                        <a href="mailto:hr-onboarding@company.com" class="text-xs text-blue-600 hover:underline mt-1 inline-block font-medium">hr-onboarding@company.com</a>
                    </div>
                </div>
            </div>

        </section>

        <!-- ================================================= -->
        <!-- RIGHT COLUMN: AI ONBOARDING BUDDY (DOCKED)         -->
        <!-- ================================================= -->
        <aside class="w-full lg:w-[420px] xl:w-[440px] flex flex-col h-[650px] lg:h-[calc(100vh-6rem)] lg:sticky lg:top-20 bg-white border border-slate-200 rounded-3xl shadow-sm overflow-hidden">
            
            <!-- Buddy Header -->
            <div class="p-4 sm:p-5 bg-gradient-to-r from-blue-600 to-indigo-600 text-white flex items-center justify-between shadow-xs shrink-0">
                <div class="flex items-center gap-3">
                    <div class="relative">
                        <div class="w-10 h-10 rounded-2xl bg-white/20 backdrop-blur-md flex items-center justify-center text-xl font-bold shadow-xs">
                            🤖
                        </div>
                        <span class="absolute -bottom-0.5 -right-0.5 w-3.5 h-3.5 rounded-full bg-emerald-400 ring-2 ring-white"></span>
                    </div>
                    <div>
                        <h2 class="font-extrabold text-base leading-tight">Onboarding Buddy</h2>
                        <p class="text-xs text-blue-100 font-medium flex items-center gap-1.5">
                            <span>Your Personal Onboarding Guide</span>
                        </p>
                    </div>
                </div>

                <div class="text-[11px] bg-white/15 px-2.5 py-1 rounded-full font-semibold border border-white/20">
                    Live Synced
                </div>
            </div>

            <!-- Quick Suggestion Chips Carousel -->
            <div class="px-4 py-3 bg-slate-50/80 border-b border-slate-100 flex gap-2 overflow-x-auto custom-scrollbar shrink-0 text-xs">
                <button onclick="sendQuickPrompt('What is my immediate next step?')" class="whitespace-nowrap px-3 py-1.5 bg-white border border-slate-200 hover:border-blue-300 hover:bg-blue-50 text-slate-700 hover:text-blue-700 rounded-full font-medium transition shadow-2xs">
                    💡 Next step?
                </button>
                <button onclick="sendQuickPrompt('What are my remaining pending trainings?')" class="whitespace-nowrap px-3 py-1.5 bg-white border border-slate-200 hover:border-blue-300 hover:bg-blue-50 text-slate-700 hover:text-blue-700 rounded-full font-medium transition shadow-2xs">
                    📚 My trainings
                </button>
                <button onclick="sendQuickPrompt('How do I complete my background verification (BGC)?')" class="whitespace-nowrap px-3 py-1.5 bg-white border border-slate-200 hover:border-blue-300 hover:bg-blue-50 text-slate-700 hover:text-blue-700 rounded-full font-medium transition shadow-2xs">
                    📄 BGC help
                </button>
                <button onclick="sendQuickPrompt('Who is my reporting manager and how do I contact them?')" class="whitespace-nowrap px-3 py-1.5 bg-white border border-slate-200 hover:border-blue-300 hover:bg-blue-50 text-slate-700 hover:text-blue-700 rounded-full font-medium transition shadow-2xs">
                    👤 Manager info
                </button>
                <button onclick="sendQuickPrompt('When will I get my IT credentials and laptop?')" class="whitespace-nowrap px-3 py-1.5 bg-white border border-slate-200 hover:border-blue-300 hover:bg-blue-50 text-slate-700 hover:text-blue-700 rounded-full font-medium transition shadow-2xs">
                    💻 Credentials
                </button>
            </div>

            <!-- Chat Messages Container -->
            <div id="chat-messages" class="flex-1 p-4 sm:p-5 overflow-y-auto space-y-4 custom-scrollbar bg-slate-50/40 text-sm">
                <!-- Welcome greeting will be loaded here -->
            </div>

            <!-- Chat Input Form -->
            <form id="chat-form" class="p-3 sm:p-4 bg-white border-t border-slate-200 flex items-center gap-2 shrink-0">
                <input type="text" id="chat-input" placeholder="Ask your Buddy anything about onboarding..." 
                       class="flex-1 px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl text-slate-800 placeholder-slate-400 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:bg-white transition" autocomplete="off">
                <button type="submit" id="send-btn" class="w-10 h-10 rounded-xl bg-blue-600 hover:bg-blue-700 text-white flex items-center justify-center shadow-md shadow-blue-600/20 transition shrink-0">
                    <svg class="w-4 h-4 translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"/>
                    </svg>
                </button>
            </form>

        </aside>

    </main>

    <!-- ===================================================== -->
    <!-- 4. JAVASCRIPT LOGIC (SYNCED WITH BACKEND)              -->
    <!-- ===================================================== -->
    <script>
        // Optional Gemini API Key
        const GEMINI_API_KEY = "AQ.Ab8RN6LSCFer5sytlGvhM4k_cLVFhDVh_JbnR7z1hiFKUuXrPA";

        // Backend endpoint
        const BACKEND_BASE_URL = window.location.origin.includes("5000") ? "" : "http://localhost:5000";

        let currentEmployee = null;
        let liveEmployeeContext = "";

        // Training icon helper
        const trainingIcons = {
            "Information Security": "🔒",
            "Code of Conduct": "📜",
            "POSH": "⚖️",
            "Data Privacy": "🛡️"
        };

        // --- NOTIFICATION SYSTEM (SEPARATED FROM CHAT) ---
        let systemNotifications = [];

        function showSystemNotification(title, message, type = 'info', badgeTime = 'Just now') {
            if (!message) return;

            // Avoid exact duplicate
            if (systemNotifications.length > 0 && systemNotifications[0].message === message) {
                return;
            }

            const timeStr = badgeTime || new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
            const notifItem = {
                id: Date.now(),
                title: title || 'Automated Action',
                message: message,
                time: timeStr,
                type: type
            };

            systemNotifications.unshift(notifItem);
            updateNotificationUI(notifItem);
        }

        function updateNotificationUI(latestItem) {
            // 1. Update High-Visibility Top Banner on Dashboard
            const banner = document.getElementById('notification-banner');
            const bannerText = document.getElementById('notif-banner-text');
            const bannerTime = document.getElementById('notif-banner-time');
            if (banner && bannerText && latestItem) {
                bannerText.innerText = latestItem.message;
                if (bannerTime) bannerTime.innerText = latestItem.time;
                banner.classList.remove('hidden');
            }

            // 2. Update Bell Badge in Navbar
            const badge = document.getElementById('notif-badge');
            const countLabel = document.getElementById('notif-count-label');
            if (badge) {
                badge.innerText = systemNotifications.length;
                badge.classList.remove('hidden');
            }
            if (countLabel) {
                countLabel.innerText = systemNotifications.length;
            }

            // 3. Update Dropdown List
            renderNotificationList();

            // 4. Show Floating Toast Alert in Top-Right
            if (latestItem) {
                showToast(latestItem);
            }
        }

        function dismissNotificationBanner() {
            const banner = document.getElementById('notification-banner');
            if (banner) banner.classList.add('hidden');
        }

        function toggleNotificationDropdown() {
            const dropdown = document.getElementById('notif-dropdown');
            if (!dropdown) return;
            dropdown.classList.toggle('hidden');
        }

        // Close dropdown when clicking outside
        document.addEventListener('click', (e) => {
            const bellBtn = document.getElementById('notif-bell-btn');
            const dropdown = document.getElementById('notif-dropdown');
            if (dropdown && !dropdown.classList.contains('hidden')) {
                if (bellBtn && !bellBtn.contains(e.target) && !dropdown.contains(e.target)) {
                    dropdown.classList.add('hidden');
                }
            }
        });

        function clearNotifications() {
            systemNotifications = [];
            const badge = document.getElementById('notif-badge');
            const countLabel = document.getElementById('notif-count-label');
            if (badge) badge.classList.add('hidden');
            if (countLabel) countLabel.innerText = '0';
            renderNotificationList();
            dismissNotificationBanner();
        }

        function renderNotificationList() {
            const list = document.getElementById('notif-dropdown-list');
            if (!list) return;

            if (systemNotifications.length === 0) {
                list.innerHTML = '<div class="p-6 text-center text-xs text-slate-400">No new notifications</div>';
                return;
            }

            list.innerHTML = systemNotifications.map(n => `
                <div class="p-3.5 hover:bg-slate-50 transition flex items-start gap-3">
                    <div class="w-7 h-7 rounded-lg bg-blue-100 text-blue-700 flex items-center justify-center text-xs shrink-0 mt-0.5">
                        ⚡
                    </div>
                    <div class="flex-1 min-w-0">
                        <div class="flex items-center justify-between gap-1">
                            <span class="text-xs font-bold text-slate-800 truncate">${escapeHtml(n.title)}</span>
                            <span class="text-[10px] text-slate-400 shrink-0">${n.time}</span>
                        </div>
                        <p class="text-xs text-slate-600 mt-0.5 leading-relaxed">${escapeHtml(n.message)}</p>
                    </div>
                </div>
            `).join('');
        }

        function showToast(item) {
            const container = document.getElementById('toast-container');
            if (!container) return;

            const toast = document.createElement('div');
            toast.className = "pointer-events-auto bg-white/95 backdrop-blur-md border border-slate-200 shadow-xl rounded-2xl p-4 flex items-start gap-3 max-w-sm w-full animate-slide-in transition-all duration-300 ring-1 ring-black/5";
            toast.innerHTML = `
                <div class="w-8 h-8 rounded-xl bg-blue-600 text-white flex items-center justify-center font-bold text-sm shadow-xs shrink-0">
                    ⚡
                </div>
                <div class="flex-1 min-w-0">
                    <div class="text-xs font-bold text-slate-900">${escapeHtml(item.title)}</div>
                    <div class="text-xs text-slate-600 mt-0.5 leading-snug">${escapeHtml(item.message)}</div>
                    <div class="text-[10px] text-slate-400 mt-1">${item.time}</div>
                </div>
                <button class="text-slate-400 hover:text-slate-700 p-1 text-xs font-bold" onclick="this.parentElement.remove()">✕</button>
            `;

            container.appendChild(toast);

            setTimeout(() => {
                if (toast.parentElement) {
                    toast.style.opacity = '0';
                    toast.style.transform = 'translateX(100%)';
                    setTimeout(() => toast.remove(), 300);
                }
            }, 6000);
        }

        // --- 1. Fetch Employee Profile & Update Dashboard ---
        async function fetchLiveEmployeeData(isManualSync = false) {
            // Check localStorage or default to Shraddha Warade
            const savedEmail = localStorage.getItem('onboarding_user_email') || "shraddhawarade85@gmail.com";
            const savedName = localStorage.getItem('onboarding_user_name') || "Shraddha Warade";

            let userRecord = null;
            let eventMsg = null;

            try {
                const response = await fetch(`${BACKEND_BASE_URL}/api/user?email=${encodeURIComponent(savedEmail)}`);
                const data = await response.json();

                if (data && data.success && data.user) {
                    userRecord = data.user;
                    eventMsg = data.event;
                }
            } catch (err) {
                console.warn("Backend not available, using local onboarding profile:", err);
            }

            // Fallback profile if PostgreSQL is offline or password failed
            if (!userRecord) {
                userRecord = {
                    name: savedName,
                    email: savedEmail,
                    profile_created: "Yes",
                    bgc_status: "Pending",
                    onboarding_status: "In Progress",
                    credentials_sent: "Pending",
                    trainings: [
                        { name: "Information Security", status: "Completed" },
                        { name: "Code of Conduct", status: "In Progress" },
                        { name: "POSH", status: "Pending" },
                        { name: "Data Privacy", status: "Pending" }
                    ],
                    pending_trainings: ["Code of Conduct", "POSH", "Data Privacy"]
                };
            }

            currentEmployee = userRecord;
            renderDashboard(userRecord, eventMsg);

            if (isManualSync) {
                showSystemNotification("Status Synchronized", "Refreshed live employee profile and onboarding milestones from database.", "info");
            }

            // Hide overlay if open
            const overlay = document.getElementById('login-overlay');
            if (overlay) {
                overlay.classList.add('hidden');
                overlay.style.display = 'none';
            }
        }

        // --- 2. Handle Profile Creation / Update ---
        async function handleProfileSubmit() {
            const nameInput = document.getElementById('employee-name-input');
            const emailInput = document.getElementById('employee-email-input');
            const statusText = document.getElementById('login-status');
            const loginBtn = document.getElementById('login-btn');

            const name = nameInput.value.trim();
            const email = emailInput.value.trim().toLowerCase();

            if (!name || !email) {
                statusText.innerText = "Please enter both your Name and Email.";
                statusText.className = "text-xs mt-4 font-semibold text-rose-500";
                return;
            }

            statusText.innerText = "Setting up your onboarding workflow...";
            statusText.className = "text-xs mt-4 font-semibold text-blue-600";
            if (loginBtn) loginBtn.disabled = true;

            // Save to browser storage
            localStorage.setItem('onboarding_user_email', email);
            localStorage.setItem('onboarding_user_name', name);

            try {
                // Register with backend database
                const res = await fetch(`${BACKEND_BASE_URL}/api/create_user`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ name: name, email: email })
                });
                const data = await res.json();
                if (data && data.success && data.user) {
                    currentEmployee = data.user;
                    renderDashboard(data.user, `Profile successfully created in database for ${name}!`);
                    closeProfileModal();
                    if (loginBtn) loginBtn.disabled = false;
                    return;
                }
            } catch (err) {
                console.warn("Backend create failed, initializing local workflow profile:", err);
            }

            // Local fallback profile
            const newProfile = {
                name: name,
                email: email,
                profile_created: "Yes",
                bgc_status: "Pending",
                onboarding_status: "In Progress",
                credentials_sent: "Pending",
                trainings: [
                    { name: "Information Security", status: "Completed" },
                    { name: "Code of Conduct", status: "In Progress" },
                    { name: "POSH", status: "Pending" },
                    { name: "Data Privacy", status: "Pending" }
                ],
                pending_trainings: ["Code of Conduct", "POSH", "Data Privacy"]
            };

            currentEmployee = newProfile;
            renderDashboard(newProfile, `Welcome ${name}! Your onboarding workflow is ready.`);
            closeProfileModal();
            if (loginBtn) loginBtn.disabled = false;
        }

        // --- 3. Render Full Dashboard (Workflow Pipeline, Stepper, Checklist) ---
        function renderDashboard(user, automationEvent) {
            const firstName = user.name ? user.name.split(' ')[0] : 'there';
            
            // Header & Avatar
            document.getElementById('nav-user-name').innerText = user.name;
            document.getElementById('nav-user-email').innerText = user.email;
            document.getElementById('hero-name').innerText = firstName;
            
            const initials = user.name.split(' ').map(n => n[0]).join('').substring(0, 2).toUpperCase();
            document.getElementById('user-avatar').innerText = initials || 'V';

            // Calculate Milestones & Progress
            let completedMilestones = 0;

            const m1Done = (user.profile_created || "").toLowerCase() === "yes";
            if (m1Done) completedMilestones++;

            const bgcStatus = (user.bgc_status || "").toLowerCase();
            const m2Done = bgcStatus === "verified" || bgcStatus === "completed";
            if (m2Done) completedMilestones++;

            const trainings = user.trainings || [];
            const completedTrainings = trainings.filter(t => (t.status || "").toLowerCase() === "completed").length;
            const totalTrainings = trainings.length || 4;
            const m3Done = completedTrainings === totalTrainings && totalTrainings > 0;
            if (m3Done) completedMilestones++;

            const m4Done = (user.credentials_sent || "").toLowerCase() === "yes";
            if (m4Done) completedMilestones++;

            const m5Done = m1Done && m2Done && m3Done && m4Done;
            if (m5Done) completedMilestones++;

            const progressPercent = Math.round((completedMilestones / 5) * 100);
            document.getElementById('progress-percent').innerText = `${progressPercent}%`;
            document.getElementById('milestones-count').innerText = `${completedMilestones} of 5 Completed`;
            document.getElementById('main-progress-bar').style.width = `${progressPercent}%`;

            // --- UPDATE WORKFLOW PIPELINE: CIRCLES & FLOWING GREEN LINES ---

            // STEP 1: Profile Creation
            setWorkflowNode(1, m1Done ? 'completed' : 'pending', 'Completed', '✓');

            // CONNECTOR LINE 1 -> 2: Moves forward if Step 1 is done
            updateFlowLine('flow-line-1', 'flow-arrow-1', m1Done);
            const mobLine2 = document.getElementById('mob-line-2');
            if (mobLine2) mobLine2.className = m1Done ? "w-0.5 h-6 bg-emerald-500 ml-5 -my-2" : "w-0.5 h-6 bg-slate-200 ml-5 -my-2";

            // STEP 2: Background Verification (BGC)
            if (m2Done) {
                setWorkflowNode(2, 'completed', 'Verified', '✓');
            } else if (bgcStatus === 'pending') {
                setWorkflowNode(2, 'active', 'Pending Review', '⏳');
            } else {
                setWorkflowNode(2, 'locked', 'Not Started', '📄');
            }

            // CONNECTOR LINE 2 -> 3: Moves forward if Step 2 is done
            updateFlowLine('flow-line-2', 'flow-arrow-2', m2Done);
            const mobLine3 = document.getElementById('mob-line-3');
            if (mobLine3) mobLine3.className = m2Done ? "w-0.5 h-6 bg-emerald-500 ml-5 -my-2" : "w-0.5 h-6 bg-slate-200 ml-5 -my-2";

            // STEP 3: Compliance Trainings
            if (m3Done) {
                setWorkflowNode(3, 'completed', 'All 4 Done', '✓');
            } else if (completedTrainings > 0) {
                setWorkflowNode(3, 'active', `${completedTrainings} of ${totalTrainings} Done`, '📚');
            } else if (m2Done) {
                setWorkflowNode(3, 'active', '0 of 4 Done', '📚');
            } else {
                setWorkflowNode(3, 'locked', '0 of 4 Done', '📚');
            }

            // CONNECTOR LINE 3 -> 4: Moves forward if Step 3 is done
            updateFlowLine('flow-line-3', 'flow-arrow-3', m3Done);
            const mobLine4 = document.getElementById('mob-line-4');
            if (mobLine4) mobLine4.className = m3Done ? "w-0.5 h-6 bg-emerald-500 ml-5 -my-2" : "w-0.5 h-6 bg-slate-200 ml-5 -my-2";

            // STEP 4: IT Credentials & System Access
            if (m4Done) {
                setWorkflowNode(4, 'completed', 'Issued & Active', '✓');
            } else if (m2Done && m3Done) {
                setWorkflowNode(4, 'active', 'Provisioning Access', '⏳');
            } else {
                setWorkflowNode(4, 'locked', 'Locked', '🔒');
            }

            // CONNECTOR LINE 4 -> 5: Moves forward if Step 4 is done
            updateFlowLine('flow-line-4', 'flow-arrow-4', m4Done);

            // STEP 5: Day 1 Ready
            if (m5Done) {
                setWorkflowNode(5, 'completed', 'Day 1 Ready! 🎉', '⭐');
            } else {
                setWorkflowNode(5, 'locked', 'Upcoming', '🚀');
            }

            // Render Trainings Checklist
            renderTrainingsList(trainings, completedTrainings, totalTrainings);

            // Build Live AI Context
            const trainingsSummary = trainings.map(t => `- ${t.name}: ${t.status}`).join('\n');
            const pendingList = user.pending_trainings && user.pending_trainings.length > 0 
                                ? user.pending_trainings.join(', ') 
                                : 'All completed!';

            liveEmployeeContext = `
Employee Name: ${user.name}
Email: ${user.email}
Account Profile Status: ${user.profile_created}
Background Check (BGC) Status: ${user.bgc_status}
Overall Onboarding Status: ${user.onboarding_status}
Work Credentials Issued: ${user.credentials_sent}

Mandatory Compliance Trainings (${completedTrainings}/${totalTrainings} completed):
${trainingsSummary}

Trainings remaining to complete: ${pendingList}

Company Information:
- Reporting Manager: Sarah Jenkins (Engineering & Onboarding Lead, email: sjenkins@company.com)
- IT Support Desk: itsupport@company.com
- HR People Operations: hr-onboarding@company.com
`;

            // Render Initial Chat Greeting
            // Send notification to dedicated Notification Banner & Dropdown, NOT into chat
            const activeEvent = automationEvent || (user.welcome_sent === 'Yes' ? `Welcome email sent to ${user.name} and recorded in database!` : null);
            if (activeEvent) {
                showSystemNotification("Automated Workflow Action", activeEvent, "success");
            } else if (systemNotifications.length === 0) {
                showSystemNotification("Onboarding Active", `Welcome ${user.name}! Your onboarding workflow is live.`, "info");
            }

            // Render Initial Chat Greeting (kept clean without system notification clutter)
            renderInitialBuddyMessage(user, completedTrainings, totalTrainings, pendingList);
        }

        // Helper to update workflow circle nodes
        function setWorkflowNode(stepNum, state, badgeText, icon) {
            const ball = document.getElementById(`step-ball-${stepNum}`);
            const badge = document.getElementById(`step-badge-${stepNum}`);
            const mobBall = document.getElementById(`mob-ball-${stepNum}`);

            if (badge) badge.innerText = badgeText;

            if (state === 'completed') {
                if (ball) {
                    ball.className = "w-14 h-14 rounded-full bg-emerald-500 text-white font-bold flex items-center justify-center shadow-lg shadow-emerald-500/25 ring-4 ring-emerald-100 text-xl transition-all duration-500 group-hover:scale-110";
                    ball.innerText = icon || '✓';
                }
                if (badge) {
                    badge.className = "mt-1 inline-block text-[10px] font-extrabold px-2 py-0.5 rounded-full bg-emerald-50 text-emerald-700 border border-emerald-200";
                }
                if (mobBall) {
                    mobBall.className = "w-10 h-10 rounded-full bg-emerald-500 text-white font-bold flex items-center justify-center shrink-0";
                    mobBall.innerText = icon || '✓';
                }
            } else if (state === 'active') {
                if (ball) {
                    ball.className = "w-14 h-14 rounded-full bg-blue-600 text-white font-bold flex items-center justify-center shadow-lg shadow-blue-600/25 ring-4 ring-blue-100 text-xl animate-pulse transition-all duration-500 group-hover:scale-110";
                    ball.innerText = icon || '⏳';
                }
                if (badge) {
                    badge.className = "mt-1 inline-block text-[10px] font-extrabold px-2 py-0.5 rounded-full bg-blue-50 text-blue-700 border border-blue-200";
                }
                if (mobBall) {
                    mobBall.className = "w-10 h-10 rounded-full bg-blue-600 text-white font-bold flex items-center justify-center shrink-0 animate-pulse";
                    mobBall.innerText = icon || '⏳';
                }
            } else {
                if (ball) {
                    ball.className = "w-14 h-14 rounded-full bg-slate-100 border-2 border-slate-200 text-slate-400 font-bold flex items-center justify-center text-xl transition-all duration-500 group-hover:scale-110";
                    ball.innerText = icon || '⚪';
                }
                if (badge) {
                    badge.className = "mt-1 inline-block text-[10px] font-extrabold px-2 py-0.5 rounded-full bg-slate-100 text-slate-500 border border-slate-200";
                }
                if (mobBall) {
                    mobBall.className = "w-10 h-10 rounded-full bg-slate-200 text-slate-400 font-bold flex items-center justify-center shrink-0";
                    mobBall.innerText = icon || '⚪';
                }
            }
        }

        // Helper to animate green line moving to next step
        function updateFlowLine(lineId, arrowId, isFilled) {
            const line = document.getElementById(lineId);
            const arrow = document.getElementById(arrowId);
            if (!line) return;

            if (isFilled) {
                setTimeout(() => {
                    line.style.width = '100%';
                    line.className = "h-full bg-emerald-500 rounded-full transition-all duration-1000 ease-out";
                }, 200);
                if (arrow) {
                    arrow.className = "absolute -top-1.5 right-0 text-[10px] text-emerald-500 font-bold transition-colors duration-500";
                }
            } else {
                line.style.width = '0%';
                if (arrow) {
                    arrow.className = "absolute -top-1.5 right-0 text-[10px] text-slate-300 font-bold transition-colors duration-500";
                }
            }
        }

        // Render Training Cards
        function renderTrainingsList(trainings, completedCount, totalCount) {
            const badge = document.getElementById('trainings-badge');
            if (badge) {
                badge.innerText = `${completedCount} of ${totalCount} Completed`;
                badge.className = completedCount === totalCount
                    ? "text-xs px-2.5 py-0.5 rounded-full font-bold bg-emerald-50 text-emerald-700"
                    : "text-xs px-2.5 py-0.5 rounded-full font-bold bg-blue-50 text-blue-700";
            }

            const grid = document.getElementById('trainings-grid');
            if (!grid) return;
            grid.innerHTML = "";

            trainings.forEach(t => {
                const isCompleted = (t.status || "").toLowerCase() === "completed";
                const isInProgress = (t.status || "").toLowerCase() === "in progress";
                const icon = trainingIcons[t.name] || "📖";

                let statusBadge = "";
                let cardBorder = "border-slate-200";

                if (isCompleted) {
                    statusBadge = `<span class="inline-flex items-center gap-1 text-[11px] font-bold px-2.5 py-0.5 rounded-full bg-emerald-50 text-emerald-700 border border-emerald-200">✓ Completed</span>`;
                    cardBorder = "border-emerald-200 bg-emerald-50/20";
                } else if (isInProgress) {
                    statusBadge = `<span class="inline-flex items-center gap-1 text-[11px] font-bold px-2.5 py-0.5 rounded-full bg-amber-50 text-amber-700 border border-amber-200">⏳ In Progress</span>`;
                    cardBorder = "border-amber-200 bg-amber-50/20";
                } else {
                    statusBadge = `<span class="inline-flex items-center gap-1 text-[11px] font-bold px-2.5 py-0.5 rounded-full bg-slate-100 text-slate-600 border border-slate-200">⚪ Pending</span>`;
                }

                grid.innerHTML += `
                    <div class="p-4 rounded-2xl border ${cardBorder} bg-white shadow-2xs hover:shadow-sm transition flex items-center justify-between gap-3 cursor-pointer" onclick="sendQuickPrompt('Tell me about my ${t.name} training status')">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 rounded-xl bg-slate-100 flex items-center justify-center text-lg">
                                ${icon}
                            </div>
                            <div>
                                <h4 class="text-sm font-bold text-slate-800">${t.name}</h4>
                                <p class="text-xs text-slate-400 font-medium">Estimated 30 mins</p>
                            </div>
                        </div>
                        <div>${statusBadge}</div>
                    </div>
                `;
            });
        }

        // Render Warm Buddy Greeting
        function renderInitialBuddyMessage(user, completedCount, totalCount, pendingList) {
            const msgs = document.getElementById('chat-messages');
            if (!msgs) return;
            msgs.innerHTML = "";

            const firstName = user.name ? user.name.split(' ')[0] : 'there';
            
            let greeting = `Hi ${firstName}! 👋 I'm your **Onboarding Buddy**.\n\n`;
           // greeting += `I'm synced with your personal onboarding workflow. You have completed **${completedCount} of ${totalCount} mandatory trainings**, and your Background Verification is **${user.bgc_status}**.\n\n`;
            
            /*if (user.bgc_status.toLowerCase() === "pending") {
                greeting += `👉 **Your immediate next milestone:** Please submit your government ID and address verification so our team can approve your profile and advance your workflow!\n\n`;
            } else if (completedCount < totalCount) {
                greeting += `👉 **Your immediate next milestone:** Please complete your remaining compliance trainings (${pendingList}).\n\n`;
            }*/

            greeting += `Ask me anything! For example: *"What should I do next?"* or *"Who is my manager?"*`;

            appendBuddyMessage(greeting, false);

// Note: System notifications are displayed in the dedicated Top Banner, Bell Dropdown & Toast, never in chat!
        }

        // Chat System
        function appendBuddyMessage(text, isUser) {
            const msgs = document.getElementById('chat-messages');
            if (!msgs) return;

            const div = document.createElement('div');
            if (isUser) {
                div.className = "flex justify-end";
                div.innerHTML = `
                    <div class="bg-blue-600 text-white px-4 py-3 rounded-2xl rounded-tr-xs max-w-[85%] text-sm shadow-sm leading-relaxed">
                        ${escapeHtml(text)}
                    </div>
                `;
            } else {
                div.className = "flex items-start gap-2.5";
                let formatted = text
                    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
                    .replace(/\*(.*?)\*/g, '<em>$1</em>')
                    .replace(/`(.*?)`/g, '<code class="bg-slate-200 px-1 py-0.5 rounded text-xs text-slate-800 font-mono">$1</code>')
                    .replace(/\n/g, '<br>');

                div.innerHTML = `
                    <div class="w-8 h-8 rounded-xl bg-gradient-to-tr from-blue-600 to-indigo-600 text-white flex items-center justify-center font-bold text-sm shrink-0 shadow-xs">
                        🤖
                    </div>
                    <div class="bg-white border border-slate-200/80 text-slate-800 px-4 py-3 rounded-2xl rounded-tl-xs max-w-[85%] text-sm shadow-2xs leading-relaxed">
                        ${formatted}
                    </div>
                `;
            }

            msgs.appendChild(div);
            msgs.scrollTop = msgs.scrollHeight;
        }

        function escapeHtml(str) {
            return str.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
        }

        function sendQuickPrompt(promptText) {
            const input = document.getElementById('chat-input');
            if (input) {
                input.value = promptText;
                submitChatPrompt();
            }
        }

        function askBuddyAboutTrainings() {
            sendQuickPrompt("Which mandatory trainings do I still need to finish?");
        }

        document.getElementById('chat-form').addEventListener('submit', (e) => {
            e.preventDefault();
            submitChatPrompt();
        });

        async function submitChatPrompt() {
            const input = document.getElementById('chat-input');
            const prompt = input.value.trim();
            if (!prompt) return;

            input.value = "";
            appendBuddyMessage(prompt, true);

            const msgs = document.getElementById('chat-messages');
            const typingId = "typing-" + Date.now();
            const typingDiv = document.createElement('div');
            typingDiv.id = typingId;
            typingDiv.className = "flex items-start gap-2.5";
            typingDiv.innerHTML = `
                <div class="w-8 h-8 rounded-xl bg-gradient-to-tr from-blue-600 to-indigo-600 text-white flex items-center justify-center font-bold text-sm shrink-0">
                    🤖
                </div>
                <div class="bg-white border border-slate-200 text-slate-400 px-4 py-3 rounded-2xl rounded-tl-xs text-xs italic shadow-2xs flex items-center gap-2">
                    <span class="w-2 h-2 rounded-full bg-blue-500 animate-ping"></span>
                    <span>Buddy is looking at your workflow...</span>
                </div>
            `;
            msgs.appendChild(typingDiv);
            msgs.scrollTop = msgs.scrollHeight;

            let responseHandled = false;

            // 1. Try Gemini AI
            if (GEMINI_API_KEY && !GEMINI_API_KEY.startsWith("AQ.Ab8RN6LSCFer5sytlGvhM4k_cLVFhDVh_JbnR7z1hiFKUuXrPA")) {
                try {
                    const systemInstructions = `
You are the Onboarding Buddy, a friendly, professional, supportive AI assistant helping a new employee throughout their onboarding workflow.
Answer questions accurately using their LIVE ONBOARDING CONTEXT below:

${liveEmployeeContext}

Guidelines:
- Speak directly to the employee in an encouraging, warm tone.
- Do NOT mention databases, SQL, PostgreSQL, or backend systems. Frame everything as their "onboarding workflow", "milestones", or "HR profile".
- Offer clear, actionable next steps.
`;
                    const res = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-lite-latest:generateContent?key=${GEMINI_API_KEY}`, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({
                            contents: [
                                { role: "user", parts: [{ text: "System Instructions:\n" + systemInstructions + "\n\nUser Question:\n" + prompt }] }
                            ]
                        })
                    });
                    const data = await res.json();
                    if (data.candidates && data.candidates.length > 0) {
                        const reply = data.candidates[0].content.parts[0].text;
                        const el = document.getElementById(typingId);
                        if (el) el.remove();
                        appendBuddyMessage(reply, false);
                        responseHandled = true;
                    }
                } catch (err) {
                    console.warn("Gemini direct request error:", err);
                }
            }

            // 2. Fallback to Python Backend /api/chat
            if (!responseHandled) {
                try {
                    const res = await fetch(`${BACKEND_BASE_URL}/api/chat`, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({
                            email: currentEmployee ? currentEmployee.email : "shraddhawarade85@gmail.com",
                            prompt: prompt
                        })
                    });
                    const data = await res.json();
                    const el = document.getElementById(typingId);
                    if (el) el.remove();
                    appendBuddyMessage(data.reply || "I couldn't retrieve that information right now. Please try again.", false);
                } catch (err) {
                    const el = document.getElementById(typingId);
                    if (el) el.remove();
                    appendBuddyMessage("I am temporarily having trouble reaching the onboarding service. Please ensure the Python server is running.", false);
                }
            }
        }

        function openProfileModal() {
            const overlay = document.getElementById('login-overlay');
            if (overlay) {
                overlay.classList.remove('hidden');
                overlay.style.display = 'flex';
                overlay.style.opacity = '1';
                const status = document.getElementById('login-status');
                if (status) status.innerText = "";
            }
        }

        function closeProfileModal() {
            const overlay = document.getElementById('login-overlay');
            if (overlay) {
                overlay.classList.add('hidden');
                overlay.style.display = 'none';
            }
        }

        // Auto-run on page load immediately!
        window.addEventListener('DOMContentLoaded', () => {
            fetchLiveEmployeeData();
        });
    </script>
</body>
</html>
